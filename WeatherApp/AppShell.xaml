<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:WeatherApp"
    x:Class="WeatherApp.AppShell">
    <ShellContent Title="Home" ContentTemplate="{DataTemplate local:MainPage}" />
    <ShellContent Title="Weather" ContentTemplate="{DataTemplate local:WeatherPage}" />
    <ShellContent Title="To-Do" ContentTemplate="{DataTemplate local:ToDoPage}" />
</Shell>
